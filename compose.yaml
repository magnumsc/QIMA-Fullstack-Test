services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=qima_fullstack_interview'
      - 'POSTGRES_PASSWORD=pass'
      - 'POSTGRES_USER=user'
    ports:
      - '5432:5432'
